'use strict';

/**
 * @class angular_module.business.modifyProductServicehttp://127.0.0.1:8080/?userId=484dd6f7-8c48-4399-8308-53f062a1b794#/
 */
angular.module('business').factory('projectContextCharacteristics', function (_) {

    var service = {
    };

    /**
     * Replace an electrical product from the list by another
     * @param oldProductPack the product to be replaced
     * @param newProductPack the new product to use
     * @returns {boolean} true if the old product pack was replaced by the new. False otherwise
     */
    service.getCharacteristics = function() {      
        return characteristics;
    };

    return service;
});
